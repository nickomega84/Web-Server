/* #pragma once
#include "iostream"

class Signals
{
	private:
	Signals(const Signals other);
	Signals operator=(const Signals other);
	void signalHandler(int signum);

	public:
	Signals();
	~Signals();
	void handleSignals();
};
 */